<% artistFromDb.forEach(artist => { %>
<div>
  <h2><%= artist.name %></h2>
  <% if (artist.images && artist.images.length > 0) { %>
  <img src="<%= artist.images[0].url %>" alt="Artist Image" />
  <% } %>
  <a href="/albums/<%= artist.id %>">Show Albums</a>
</div>
<% }); %>
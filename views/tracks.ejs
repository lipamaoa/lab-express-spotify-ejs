<h1>Tracks</h1>
<% tracks.forEach(track => { %>
  <div>
    <h2><%- track.name %></h2>
    <audio controls>
        <source src="<%=track.preview_url %>" type="audio/mpeg">
    </audio>
     </div>
<% }) %>